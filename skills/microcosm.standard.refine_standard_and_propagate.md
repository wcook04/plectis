# Microcosm Standard Refine Standard And Propagate

Use this public-safe skill projection only when std_microcosm_standard itself must change and the change must propagate through every standard row, skill triad, validator, generated projection, and health check.

## Typed Skill Mapping

```json
{
  "triad_role": "refine_standard_and_propagate",
  "operates_standard": "std_microcosm_standard",
  "acts_on_kind": "standard",
  "trigger_summary": [
    "A reusable invariant belongs in std_microcosm_standard rather than one standard row.",
    "Changing the standard meta-contract alters required fields, edge contracts, validator expectations, omission receipts, source-coupling rules, or authority ceilings across the standard corpus."
  ],
  "workflow_summary": [
    "Prove the invariant from live standard JSON rows, standards registry, relation registry, validators, generated health, and receipt evidence before editing the meta-standard.",
    "Update builder and validator logic so governs-kind edges, triad-skill pressure, contract residuals, anti-claims, and claim ceilings are computed rather than asserted.",
    "Regenerate the lattice and keep unsupported validator completeness, source-body equivalence, release, or downstream-propagation claims as residual pressure until source evidence resolves them."
  ],
  "concept_refs": [
    "concept.standards_meta_diagnostics_bundle"
  ],
  "mechanism_refs": [
    "mechanism.standards_meta_diagnostics.validates_public_standards_meta_diagnostics"
  ],
  "mapping_basis": "This markdown maps standard meta-contract propagation to std_microcosm_standard, the standard doctrine kind, and standards-meta diagnostics because changes to the recursive standard must be validated across the generated standard corpus before they govern downstream artifacts."
}
```

## Authority Boundary

This skill source governs standard meta-contract propagation only. It does not make standard JSON parity, resolved triad edges, graph reachability, or generated health status into proof that every standard is complete, validated, runtime-safe, or release-ready.

Anti-claims:

- Do not weaken std_microcosm_standard to make health greener.
- Do not convert generated projection availability into validator completeness, source-body, receipt, downstream propagation, or release support.
- Do not close standard propagation without builder parity, validator checks, and scoped receipt binding.
