# Microcosm Tool Server Pressure Inventory Author

Use this public-safe skill when the accepted tool-server pressure inventory standard needs an instance authored from synthetic process fixtures, redaction checks, source-module digests, negative cases, body-free receipts, and explicit authority ceilings.

## Typed Skill Mapping

```json
{
  "triad_role": "author",
  "operates_standard": "std_microcosm_tool_server_pressure_inventory",
  "acts_on_kind": "tool_server_pressure_inventory",
  "trigger_summary": [
    "An accepted tool-server pressure inventory standard declares this author skill and needs a governed instance instead of planned route pressure.",
    "The instance can be authored from public synthetic process fixtures, source-module manifests, redaction checks, negative cases, body-free receipts, and authority ceilings."
  ],
  "workflow_summary": [
    "Read the standard JSON, organ atlas row, runtime source locus, exported pressure-inventory bundle, source-module manifest, mechanism source, receipts, and generated lattice health.",
    "Author only public fixture, source-module, mechanism, validator, receipt, authority-boundary, redaction, and anti-claim fields.",
    "Regenerate the skill corpus and lattice projection, keeping live process reads, process signalling, host mutation, provider calls, release, and private-data equivalence outside the claim ceiling."
  ],
  "concept_refs": [
    "concept.executable_doctrine_grammar_standard_bundle"
  ],
  "mechanism_refs": [
    "mechanism.tool_server_pressure_inventory.validates_public_tool_server_pressure_inventory"
  ],
  "mapping_basis": "The accepted standard declares this exact author skill id, and the organ atlas row resolves the tool-server pressure mechanism through public synthetic process fixtures, redaction checks, source-module digests, negative cases, body-free receipts, and authority ceilings."
}
```

## Authority Boundary

This skill governs public tool-server pressure inventory authoring only. It does not authorize live process reads, process signalling, host mutation, provider calls, release, private-data equivalence, or whole-system correctness claims.

Anti-claims:

- Synthetic process fixture validation is not live host authority.
- Redaction and negative-case checks cannot become proof of runtime safety.
- Generated projection parity is not evidence for release or private equivalence.
