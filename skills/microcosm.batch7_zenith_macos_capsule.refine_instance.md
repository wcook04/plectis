# Microcosm Batch7 Zenith macOS Capsule Refine Instance

Use this public-safe skill when the Batch7 Zenith macOS Capsule standard needs a governed instance refinement from source refs, residual pressure, validator output, or receipt evidence rather than broad prose.

## Typed Skill Mapping

```json
{
  "triad_role": "refine_instance",
  "operates_standard": "std_microcosm_batch7_zenith_macos_capsule",
  "acts_on_kind": "batch7_zenith_macos_capsule",
  "trigger_summary": [
    "The std_microcosm_batch7_zenith_macos_capsule contract declares this refine_instance skill and needs a governed instance refinement from source refs, residual pressure, validator output, or receipt evidence rather than broad prose.",
    "The batch7_zenith_macos_capsule lane can be worked from its standard JSON, registry row, source refs, public fixtures or paper-module boundary, and explicit authority ceiling."
  ],
  "workflow_summary": [
    "Read the standard JSON, current generated skill instance, standards registry row, generated health gaps, validator output, and any receipt refs before refining.",
    "Tighten only source-backed fields, claim ceilings, omission receipts, validator refs, and typed residuals that the source surface can support.",
    "Regenerate the skill corpus and doctrine lattice, leaving unsupported mechanism neighbours, runtime correctness, release readiness, and whole-system correctness as residual pressure."
  ],
  "concept_refs": [
    "concept.voice_to_doctrine_self_improvement_loop_bundle"
  ],
  "mechanism_refs": [
    "mechanism.batch7_zenith_macos_capsule.validates_public_zenith_macos_capsule"
  ],
  "mapping_basis": "The standard JSON for std_microcosm_batch7_zenith_macos_capsule declares this exact refine_instance skill id and governed kind batch7_zenith_macos_capsule; core/mechanism_sources.json names mechanism.batch7_zenith_macos_capsule.validates_public_zenith_macos_capsule as the active mechanism, and core/paper_module_capsules.json binds that mechanism as the Zenith capsule subject without accepted-organ authority. This binds instance refinement to the mechanism-backed public Swift app-shell capsule and the standard-contract concept without claiming app launch, macOS permission, backend-control, release, complete UI coverage, or standalone organ authority."
}
```

## Authority Boundary

This skill governs batch7_zenith_macos_capsule instance refinement only. It does not certify runtime correctness, source-body equivalence, provider behavior, release readiness, publication readiness, private-root equivalence, or whole-system correctness. The only mechanism neighbour asserted here is the active Zenith macOS capsule mechanism named by the mechanism registry and paper-module capsule subject; no accepted-organ, app-launch, macOS-permission, backend-control, release, or complete-UI authority is minted.

Anti-claims:

- The standard triad edge is route authority, not proof the governed artifact is complete.
- Source refs and fixtures are evidence boundaries, not release or runtime-correctness proof.
- The mechanism neighbour is limited to the active Zenith macOS capsule mechanism named by source authority; it is not accepted-organ, app-launch, macOS-permission, backend-control, release, or complete-UI authority.
- Generated projections are not source evidence.
