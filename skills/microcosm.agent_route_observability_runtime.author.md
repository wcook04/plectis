# Microcosm Agent Route Observability Runtime Author

Use this public-safe skill when the accepted agent-route observability runtime standard needs an instance authored from public route traces, copied non-secret observability source bodies, source-module manifests, receipts, and explicit claim ceilings.

## Typed Skill Mapping

```json
{
  "triad_role": "author",
  "operates_standard": "std_microcosm_agent_route_observability_runtime",
  "acts_on_kind": "agent_route_observability_runtime",
  "trigger_summary": [
    "An accepted route-observability runtime standard declares this author skill and needs a governed instance rather than planned route pressure.",
    "The instance can be authored from public route traces, copied non-secret macro trace bodies, source-module manifests, route repair fixtures, body-free receipts, and authority ceilings."
  ],
  "workflow_summary": [
    "Read the standard JSON, organ atlas row, runtime source locus, exported route-observability bundle, source-module manifest, mechanism source, receipts, and generated lattice health.",
    "Author only source-backed public fields: route trace refs, copied-source refs, source-module refs, mechanism refs, validator refs, receipt expectations, authority boundary, and anti-claims.",
    "Regenerate the skill corpus and lattice projection, then keep live session behavior, private traces, provider execution, browser authority, release readiness, and whole-system correctness outside the claim ceiling."
  ],
  "concept_refs": [
    "concept.executable_doctrine_grammar_standard_bundle"
  ],
  "mechanism_refs": [
    "mechanism.agent_route_observability_runtime.validates_public_route_feedback"
  ],
  "mapping_basis": "The accepted standard declares this exact author skill id, and the organ atlas row resolves the route-observability mechanism through public route traces, copied non-secret source bodies, source-module manifests, route repair fixtures, body-free receipts, and an authority ceiling."
}
```

## Authority Boundary

This skill governs public route-observability runtime authoring only. It does not certify live session behavior, private trace access, provider execution, browser authority, release readiness, or whole-system correctness.

Anti-claims:

- Public route traces are route evidence, not live session authority.
- Copied non-secret source bodies cannot be laundered into private trace coverage.
- Generated projections are not release evidence.
