# Microcosm Standard Author

Use this public-safe skill projection when a Microcosm standard needs governed JSON-contract admission for a doctrine kind. It is not a route for turning a README, manifest excerpt, style guide, or generated projection into a validator-ready standard.

## Typed Skill Mapping

```json
{
  "triad_role": "author",
  "operates_standard": "std_microcosm_standard",
  "acts_on_kind": "standard",
  "trigger_summary": [
    "A doctrine kind needs an executable public-safe standard with required fields, lattice edges, validation rules, omission receipts, anti-claims, and authority ceiling.",
    "The agent can name the governed kind, source refs, source-coupling boundary, required/selective relation contract, skill triad ids, validators, and projection behavior from JSON source evidence."
  ],
  "workflow_summary": [
    "Read std_microcosm_standard, the candidate standard JSON, standards registry, relation registry, kind envelope, generated health, and validator refs before editing.",
    "Admit only JSON-backed standard contracts or typed residual pressure; do not infer schema authority, source-body equivalence, or release readiness from manifest id refs.",
    "Regenerate standard, skill, and doctrine-lattice projections, then run standard, skill, and broad projection checks before scoped commit."
  ],
  "concept_refs": [
    "concept.executable_doctrine_grammar_standard_bundle"
  ],
  "mechanism_refs": [
    "mechanism.executable_doctrine_grammar.validates_public_doctrine_grammar_bundle"
  ],
  "mapping_basis": "This markdown binds standard authoring to std_microcosm_standard, the standard doctrine kind, and executable doctrine grammar because standards must enter as JSON contracts with computed required edges, validators, source boundaries, and anti-claims rather than broad prose."
}
```

## Authority Boundary

This skill source governs standard admission only. A standard JSON row is a schema and validator contract; it does not copy private source bodies, prove implementation correctness, authorize release, or make generated projection counts into source doctrine.

Anti-claims:

- A manifest id, README, or generated projection is not enough to infer a complete standard contract.
- Standard JSON parity does not prove validator completeness, runtime correctness, public/private equivalence, or release readiness.
- This recursive route must not weaken std_microcosm_standard merely to make standard-triad counts greener.
