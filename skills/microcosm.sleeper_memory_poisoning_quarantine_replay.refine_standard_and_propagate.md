# Microcosm Sleeper Memory Poisoning Quarantine Replay Refine Standard And Propagate

Use this public-safe skill only when a reusable sleeper memory poisoning quarantine replay invariant must change at the standard level and propagate through governed instances, validators, receipts, mechanisms, and lattice projections.

## Typed Skill Mapping

```json
{
  "triad_role": "refine_standard_and_propagate",
  "operates_standard": "std_microcosm_sleeper_memory_poisoning_quarantine_replay",
  "acts_on_kind": "sleeper_memory_poisoning_quarantine_replay",
  "trigger_summary": [
    "A recurring sleeper memory poisoning replay rule about synthetic fixtures, quarantine refs, source-module manifests, receipts, or authority ceilings belongs at the standard level.",
    "The change must propagate through instances, validators, receipts, generated lattice projections, and anti-claims without asserting live memory security authority."
  ],
  "workflow_summary": [
    "Prove the invariant from the standard, organ registry row, fixture protocol, mechanism evidence, exported bundle, source-module manifest, validator evidence, and receipts.",
    "Update source standards, validators, receipts, generated projections, and focused checks together so the rule is computed and not asserted.",
    "Leave unsupported live-memory-security, private-memory, provider execution, release, and whole-system correctness claims as explicit residual pressure."
  ],
  "concept_refs": [
    "concept.voice_to_doctrine_self_improvement_loop_bundle"
  ],
  "mechanism_refs": [
    "mechanism.sleeper_memory_poisoning_quarantine_replay.validates_public_sleeper_memory_poisoning_quarantine_replay"
  ],
  "mapping_basis": "The standard declares this exact propagate skill id, and the resolved mechanism proves the public sleeper memory poisoning quarantine replay boundary through synthetic fixtures, source-module manifests, validators, negative cases, and receipts without making live-memory-security claims."
}
```

## Authority Boundary

This skill governs class-level sleeper memory poisoning quarantine replay propagation only. It does not certify live memory security, private memory state, provider execution, release readiness, or whole-system correctness.

Anti-claims:

- Standard propagation cannot lower per-instance evidence requirements.
- Generated health improvement is not proof of replay correctness.
- Source-module and receipt evidence must keep its stated claim ceiling.
