# Microcosm Mechanism Refine Instance

Use this public-safe skill projection when an existing mechanism instance needs a bounded correction to source grounding, host routing, law boundary, receipt binding, or residual pressure.

## Typed Skill Mapping

```json
{
  "triad_role": "refine_instance",
  "operates_standard": "std_microcosm_mechanism",
  "acts_on_kind": "mechanism",
  "trigger_summary": [
    "A mechanism has an unresolved code locus, host organ, law boundary, receipt, or selective edge that can be fixed from source evidence.",
    "The refinement can be validated without making generated projection output into evidence."
  ],
  "workflow_summary": [
    "Inspect mechanism_sources, generated mechanism JSON, related organ rows, code loci, receipts, and health deficits.",
    "Mutate source authority or validators only; route unsupported claims as typed residual pressure.",
    "Regenerate through build_doctrine_projection.py and rerun focused checks before committing."
  ],
  "concept_refs": [
    "concept.voice_to_doctrine_self_improvement_loop_bundle"
  ],
  "mechanism_refs": [
    "mechanism.doctrine_fact_claim_audit.validates_public_doctrine_fact_claim_audit"
  ],
  "mapping_basis": "This markdown maps mechanism refinement to std_microcosm_mechanism, the mechanism doctrine kind, and the doctrine fact-claim audit mechanism because refinements must separate positive support from residual or rejected claims."
}
```

## Authority Boundary

This skill source is a route for improving one mechanism instance. It does not authorize blanket law wiring or treating endpoint and negative-case coverage as propagation proof.

Anti-claims:

- Resolved code or receipt refs are support boundaries, not whole-mechanism correctness.
- Anti-claim rejection stays separate from positive support.
- The generated projection remains a computed view, not source authority.
