# Microcosm Sleeper Memory Poisoning Quarantine Replay Author

Use this public-safe skill when the sleeper memory poisoning quarantine replay standard needs an instance authored from synthetic memory poisoning fixtures, quarantine decisions, source-module manifests, receipts, validators, and explicit authority ceilings.

## Typed Skill Mapping

```json
{
  "triad_role": "author",
  "operates_standard": "std_microcosm_sleeper_memory_poisoning_quarantine_replay",
  "acts_on_kind": "sleeper_memory_poisoning_quarantine_replay",
  "trigger_summary": [
    "The sleeper memory poisoning quarantine replay standard declares this author skill and needs a governed instance rather than planned route pressure.",
    "The instance can be authored from synthetic memory poisoning fixtures, quarantine evidence, source-module manifests, validators, negative cases, and receipts without claiming live memory security authority."
  ],
  "workflow_summary": [
    "Read the standard JSON, organ registry row, fixture protocol, exported runtime bundle, source-module manifest, mechanism source, receipts, validator refs, and generated lattice health.",
    "Author only source-backed public fields: poisoning refs, quarantine refs, source-module refs, mechanism refs, validator refs, receipt expectations, authority boundary, and anti-claims.",
    "Regenerate the skill corpus and lattice projection, then keep live memory security, private memory state, provider execution, release readiness, and whole-system correctness outside the claim ceiling."
  ],
  "concept_refs": [
    "concept.executable_doctrine_grammar_standard_bundle"
  ],
  "mechanism_refs": [
    "mechanism.sleeper_memory_poisoning_quarantine_replay.validates_public_sleeper_memory_poisoning_quarantine_replay"
  ],
  "mapping_basis": "The standard declares this exact author skill id, and the resolved mechanism validates the public sleeper memory poisoning quarantine replay boundary through synthetic fixtures, source-module manifests, validators, negative cases, and receipts."
}
```

## Authority Boundary

This skill governs public sleeper memory poisoning quarantine replay authoring only. It does not certify live memory security, private memory state, provider execution, release readiness, or whole-system correctness.

Anti-claims:

- A resolved triad skill edge is route coverage, not live memory-security proof.
- Synthetic poisoning fixtures cannot be laundered into live memory-product evidence.
- Generated lattice projections are not release evidence.
