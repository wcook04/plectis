# Microcosm Skill Refine Instance

Use this public-safe skill projection when an existing Microcosm skill needs a bounded correction to triggers, workflow, typed mapping, validation refs, doctrine neighbours, omission receipt, anti-claims, or migration boundary. It is not a route for silently upgrading generated JSON into source authority.

## Typed Skill Mapping

```json
{
  "triad_role": "refine_instance",
  "operates_standard": "std_microcosm_skill",
  "acts_on_kind": "skill",
  "trigger_summary": [
    "A governed skill row has stale Markdown digest parity, weak typed mapping evidence, missing validation route, unclear authority ceiling, or unsupported selective doctrine neighbours.",
    "The refinement can be scoped from the skill Markdown source, generated JSON projection, relation registry, validator behavior, or generated health without changing std_microcosm_skill itself."
  ],
  "workflow_summary": [
    "Inspect the skill Markdown source, generated JSON, operated standard, acted-on kind, concept and mechanism refs, relation registry, generated health, and validator refs before mutation.",
    "Change only source wording or validator behavior that narrows the claim ceiling or resolves a named standard/kind/doctrine edge from evidence.",
    "Regenerate through the builder and leave unsupported mechanism, concept, runtime uptake, or source-migration claims as typed residual pressure."
  ],
  "concept_refs": [
    "concept.voice_to_doctrine_self_improvement_loop_bundle"
  ],
  "mechanism_refs": [
    "mechanism.doctrine_fact_claim_audit.validates_public_doctrine_fact_claim_audit"
  ],
  "mapping_basis": "This markdown maps skill instance refinement to std_microcosm_skill, the skill doctrine kind, and doctrine fact-claim audit because skill corrections must tighten typed route evidence and authority ceilings instead of laundering generated projection reachability."
}
```

## Authority Boundary

This skill source governs bounded skill-instance correction only. It does not authorize source-of-record migration, private prompt/provider body exposure, release authority, or broad doctrine wiring without source evidence.

Anti-claims:

- Count movement is not proof that every skill has complete concept, mechanism, validation, and runtime neighbours.
- Generated JSON parity or catalog visibility is not agent adoption proof.
- Generated health can expose the gap, but Markdown source evidence, validator computation, or a typed residual must settle it.
